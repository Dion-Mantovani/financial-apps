---
import Layout from '../layouts/Layout.astro';
import {
  ArrowLeft,
  Plus,
  Sparkles,
  X,
  Pencil,
  Trash2,
  Utensils,
  Car,
  ShoppingBag,
  Briefcase,
  Gift,
  HeartPulse,
  Gamepad2,
  TrendingUp,
  TrendingDown,
  ChevronRight,
  AlertTriangle,
  Receipt,
  CircleEllipsis,
  Wallet,
  Zap,
  Droplets,
  Wifi,
  Home,
  Shirt,
  Coffee,
  Film,
  Dumbbell,
  Scissors,
  GraduationCap,
  Baby,
  PawPrint,
  Plane,
  Fuel,
  Wrench,
  PiggyBank,
  CreditCard,
  Coins,
  Building,
  Tag,
} from '@lucide/astro';
---

<Layout title="Manajemen Kategori - Studion App">
  <div
    x-data="{
       catStore: Alpine.store('categoryStore'),
  txStore: Alpine.store('transactionStore'),
       activeTab: 'expense',
  openFormModal: false,
  openDetailModal: false,
  openReassignModal: false,
  openDeleteConfirmModal: false,
  openIconPicker: false,
  isEdit: false,

  selectedCategory: {
    id: '',
    name: '',
    type: 'expense',
    icon: 'utensils'
  },

  formData: {
    id: null,
    name: '',
    type: 'expense',
    icon: 'utensils'
  },

  targetCategoryId: '',

      // Master List Ikon beserta Pemetaan Tipe
      availableIcons: [
        // --- EXPENSE ICONS ---
        { id: 'utensils', name: 'Makanan', type: 'expense' },
        { id: 'car', name: 'Transport', type: 'expense' },
        { id: 'shopping-bag', name: 'Belanja', type: 'expense' },
        { id: 'gamepad-2', name: 'Hiburan', type: 'expense' },
        { id: 'heart-pulse', name: 'Kesehatan', type: 'expense' },
        { id: 'receipt', name: 'Tagihan', type: 'expense' },
        { id: 'zap', name: 'Listrik', type: 'expense' },
        { id: 'droplets', name: 'Air', type: 'expense' },
        { id: 'wifi', name: 'Internet', type: 'expense' },
        { id: 'home', name: 'Rumah', type: 'expense' },
        { id: 'coffee', name: 'Kopi', type: 'expense' },
        { id: 'shirt', name: 'Fashion', type: 'expense' },
        { id: 'film', name: 'Streaming', type: 'expense' },
        { id: 'dumbbell', name: 'Olahraga', type: 'expense' },
        { id: 'scissors', name: 'Perawatan', type: 'expense' },
        { id: 'graduation-cap', name: 'Pendidikan', type: 'expense' },
        { id: 'baby', name: 'Anak', type: 'expense' },
        { id: 'paw-print', name: 'Peliharaan', type: 'expense' },
        { id: 'plane', name: 'Travel', type: 'expense' },
        { id: 'fuel', name: 'Bensin', type: 'expense' },
        { id: 'wrench', name: 'Servis', type: 'expense' },
        { id: 'credit-card', name: 'Cicilan', type: 'expense' },

        // --- INCOME ICONS ---
        { id: 'briefcase', name: 'Pekerjaan', type: 'income' },
        { id: 'gift', name: 'Hadiah', type: 'income' },
        { id: 'trending-up', name: 'Investasi', type: 'income' },
        { id: 'wallet', name: 'Gaji', type: 'income' },
        { id: 'piggy-bank', name: 'Tabungan', type: 'income' },
        { id: 'coins', name: 'Passive', type: 'income' },
        { id: 'building', name: 'Sewa', type: 'income' },

        // --- KEDUA TIPE ---
        { id: 'circle-ellipsis', name: 'Lainnya', type: 'both' },
      ],

      async init() {
        if (!this.catStore.categories.length) {
          await this.catStore.init();
        }
        if (!this.txStore.transactions.length) {
          await this.txStore.init();
        }
      },

      get filteredCategories() {
          return [...this.catStore.categories]
          .filter(c => c.type === this.activeTab)
          .sort((a, b) => {
            return new Date(b.created_at || 0) - new Date(a.created_at || 0);
          });
      },

      get availableReassignCategories() {
        return this.catStore.categories.filter(c => c.type === this.selectedCategory.type && c.id !== this.selectedCategory.id);
      },

      get usedTransactionCount() {
        if (!this.selectedCategory.id) return 0;
        return this.txStore.transactions.filter(tx => tx.category_id === this.selectedCategory.id).length;
      },

      // Filter Ikon Sesuai Tipe yang Dipilih di Form
      get filteredAvailableIcons() {
        return this.availableIcons.filter(
          (icon) => icon.type === this.formData.type || icon.type === 'both'
        );
      },

      setFormType(type) {
        this.formData.type = type;
        // Validasi: Jika ikon saat ini tidak ada di tipe baru, reset ke default tipe tersebut
        if (!this.filteredAvailableIcons.some(i => i.id === this.formData.icon)) {
          this.formData.icon = type === 'expense' ? 'utensils' : 'wallet';
        }
      },

      showDetail(cat) {
        this.selectedCategory = { ...cat };
        this.openDetailModal = true;
      },

      openAddModal() {
        this.isEdit = false;

        this.formData = {
          id: null,
          name: '',
          type: this.activeTab,
          icon: this.activeTab === 'expense' ? 'utensils' : 'wallet'
        };

        this.openIconPicker = true;
        this.openFormModal = true;

        this.$nextTick(() => {
          this.$refs.categoryNameInput?.focus();
        });
      },

      openEditFromDetail() {
        this.isEdit = true;
        this.formData = {
          ...this.selectedCategory
        };
        this.openDetailModal = false;
        this.openIconPicker = true;
        this.openFormModal = true;
      },

     async attemptDeleteCategory() {
  if (this.usedTransactionCount > 0) {
    this.openDetailModal = false;
    this.openReassignModal = true;
    return;
  }

  await this.catStore.deleteCategory(this.selectedCategory.id);
  this.openDetailModal = false;
},




      async saveCategory() {
        if (!this.formData.name.trim()) return;

        if (this.isEdit) {
          await this.catStore.updateCategory(this.formData.id, {
            name: this.formData.name.trim(),
            type: this.formData.type,
            icon: this.formData.icon
          });
        } else {
          await this.catStore.addCategory({
            name: this.formData.name.trim(),
            type: this.formData.type,
            icon: this.formData.icon
          });
        }
        this.openFormModal = false;
      }
    }"
    class="space-y-5 pt-2 pb-12"
  >
    <!-- 1. STICKY HEADER -->
    <div
      class="border-border bg-background/95 sticky top-0 z-40 -mx-5 -mt-2 space-y-3 border-b px-5 pt-6 pb-3 backdrop-blur-md"
    >
      <div class="flex items-center justify-between">
        <a
          href="/"
          class="border-border bg-card text-muted-foreground hover:bg-muted flex h-9.5 w-9.5 cursor-pointer items-center justify-center rounded-2xl border shadow-xs transition-all active:scale-95"
        >
          <ArrowLeft class="h-4.5 w-4.5 stroke-[2.5]" />
        </a>

        <h1 class="text-primary text-base font-extrabold tracking-tight">
          Kategori Transaksi
        </h1>

        <div class="w-9.5"></div>
      </div>
    </div>

    <!-- 2. TAB SELECTOR -->
    <div
      class="border-border bg-card grid grid-cols-2 gap-2 rounded-2xl border p-1 shadow-xs"
    >
      <button
        type="button"
        @click="activeTab = 'expense'"
        :class="activeTab === 'expense' ? 'bg-primary text-primary-foreground shadow-soft' : 'text-muted-foreground hover:text-foreground'"
        class="flex cursor-pointer items-center justify-center gap-2 rounded-xl py-2 text-xs font-bold transition-all"
      >
        <TrendingDown class="h-3.5 w-3.5 stroke-[2.5]" />
        <span>Pengeluaran</span>
      </button>

      <button
        type="button"
        @click="activeTab = 'income'"
        :class="activeTab === 'income' ? 'bg-primary text-primary-foreground shadow-soft' : 'text-muted-foreground hover:text-foreground'"
        class="flex cursor-pointer items-center justify-center gap-2 rounded-xl py-2 text-xs font-bold transition-all"
      >
        <TrendingUp class="h-3.5 w-3.5 stroke-[2.5]" />
        <span>Pemasukan</span>
      </button>
    </div>

    <!-- 4. DAFTAR KATEGORI LIST -->
    <div class="grid grid-cols-2 gap-3">
      <template x-for="category in filteredCategories" :key="category.id">
        <button
          type="button"
          @click="showDetail(category)"
          class="border-border bg-card hover:bg-muted/40 shadow-soft flex items-center gap-3 rounded-[22px] border p-4 text-left transition-colors"
        >
          <!-- Category Icon -->
          <div
            class="bg-blue-soft text-blue flex h-10 w-10 shrink-0 items-center justify-center rounded-2xl"
          >
            <template x-if="category.icon === 'utensils'">
              <Utensils :size="18" />
            </template>

            <template x-if="category.icon === 'car'">
              <Car :size="18" />
            </template>

            <template x-if="category.icon === 'shopping-bag'">
              <ShoppingBag :size="18" />
            </template>

            <template x-if="category.icon === 'gamepad-2'">
              <Gamepad2 :size="18" />
            </template>

            <template x-if="category.icon === 'heart-pulse'">
              <HeartPulse :size="18" />
            </template>

            <template x-if="category.icon === 'receipt'">
              <Receipt :size="18" />
            </template>

            <template x-if="category.icon === 'zap'">
              <Zap :size="18" />
            </template>

            <template x-if="category.icon === 'droplets'">
              <Droplets :size="18" />
            </template>

            <template x-if="category.icon === 'wifi'">
              <Wifi :size="18" />
            </template>

            <template x-if="category.icon === 'home'">
              <Home :size="18" />
            </template>

            <template x-if="category.icon === 'coffee'">
              <Coffee :size="18" />
            </template>

            <template x-if="category.icon === 'shirt'">
              <Shirt :size="18" />
            </template>

            <template x-if="category.icon === 'film'">
              <Film :size="18" />
            </template>

            <template x-if="category.icon === 'dumbbell'">
              <Dumbbell :size="18" />
            </template>

            <template x-if="category.icon === 'scissors'">
              <Scissors :size="18" />
            </template>

            <template x-if="category.icon === 'graduation-cap'">
              <GraduationCap :size="18" />
            </template>

            <template x-if="category.icon === 'baby'">
              <Baby :size="18" />
            </template>

            <template x-if="category.icon === 'paw-print'">
              <PawPrint :size="18" />
            </template>

            <template x-if="category.icon === 'plane'">
              <Plane :size="18" />
            </template>

            <template x-if="category.icon === 'fuel'">
              <Fuel :size="18" />
            </template>

            <template x-if="category.icon === 'wrench'">
              <Wrench :size="18" />
            </template>

            <template x-if="category.icon === 'credit-card'">
              <CreditCard :size="18" />
            </template>

            <template x-if="category.icon === 'briefcase'">
              <Briefcase :size="18" />
            </template>

            <template x-if="category.icon === 'gift'">
              <Gift :size="18" />
            </template>

            <template x-if="category.icon === 'trending-up'">
              <TrendingUp :size="18" />
            </template>

            <template x-if="category.icon === 'wallet'">
              <Wallet :size="18" />
            </template>

            <template x-if="category.icon === 'piggy-bank'">
              <PiggyBank :size="18" />
            </template>

            <template x-if="category.icon === 'coins'">
              <Coins :size="18" />
            </template>

            <template x-if="category.icon === 'building'">
              <Building :size="18" />
            </template>

            <template x-if="category.icon === 'circle-ellipsis'">
              <CircleEllipsis :size="18" />
            </template>
          </div>

          <!-- Category Name -->
          <span
            class="text-foreground min-w-0 truncate text-sm font-medium"
            x-text="category.name"></span>
        </button>
      </template>
    </div>

    <button
      type="button"
      @click="openAddModal()"
      class="border-blue-main/40 bg-blue-soft/30 text-blue-main hover:border-blue-main hover:bg-blue-soft flex w-full cursor-pointer items-center justify-center gap-2 rounded-2xl border border-dashed py-3.5 shadow-xs transition-all active:scale-95"
    >
      <Plus class="h-4 w-4 stroke-[2.5]" />
      <span class="text-xs font-bold">Tambah Kategori Baru</span>
    </button>

    <!-- 5. MODAL DETAIL KATEGORI -->
    <div
      x-show="openDetailModal"
      x-cloak
      class="bg-primary/20 fixed inset-0 z-50 flex items-end justify-center backdrop-blur-xs transition-opacity sm:items-center"
    >
      <div
        @click.away="openDetailModal = false"
        class="border-border bg-card flex max-h-[90vh] w-full max-w-md flex-col overflow-hidden rounded-t-4xl border pb-14 shadow-2xl transition-transform sm:rounded-4xl"
      >
        <div class="bg-card-blue-dark relative p-6 text-white">
          <div
            class="pointer-events-none absolute -right-6 -bottom-8 h-28 w-28 rounded-full bg-white/10"
          >
          </div>

          <div class="relative z-10 flex items-center justify-between">
            <span
              x-text="selectedCategory.type === 'expense' ? 'Kategori Pengeluaran' : 'Kategori Pemasukan'"
              class="rounded-full bg-white/20 px-2.5 py-0.5 text-[9px] font-bold tracking-widest text-white uppercase"
            ></span>
            <button
              type="button"
              @click="openDetailModal = false"
              class="flex h-8 w-8 items-center justify-center rounded-full bg-white/20 text-white"
            >
              <X class="h-4 w-4 stroke-[2.5]" />
            </button>
          </div>

          <div class="relative z-10 mt-5 flex items-center gap-3.5">
            <div
              class="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-white/20 text-white"
            >
              <template x-if="selectedCategory.icon === 'utensils'"
                ><Utensils class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'car'"
                ><Car class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'shopping-bag'"
                ><ShoppingBag class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'gamepad-2'"
                ><Gamepad2 class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'heart-pulse'"
                ><HeartPulse class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'briefcase'"
                ><Briefcase class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'gift'"
                ><Gift class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'receipt'"
                ><Receipt class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'circle-ellipsis'"
                ><CircleEllipsis class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'trending-up'"
                ><TrendingUp class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'wallet'"
                ><Wallet class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'zap'"
                ><Zap class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'droplets'"
                ><Droplets class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'wifi'"
                ><Wifi class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'home'"
                ><Home class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'shirt'"
                ><Shirt class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'coffee'"
                ><Coffee class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'film'"
                ><Film class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'dumbbell'"
                ><Dumbbell class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'scissors'"
                ><Scissors class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'graduation-cap'"
                ><GraduationCap class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'baby'"
                ><Baby class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'paw-print'"
                ><PawPrint class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'plane'"
                ><Plane class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'fuel'"
                ><Fuel class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'wrench'"
                ><Wrench class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'piggy-bank'"
                ><PiggyBank class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'credit-card'"
                ><CreditCard class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'coins'"
                ><Coins class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'building'"
                ><Building class="h-6 w-6 stroke-2" /></template
              >
              <template x-if="selectedCategory.icon === 'tag'"
                ><Tag class="h-6 w-6 stroke-2" /></template
              >
            </div>

            <div class="flex min-w-0 flex-col">
              <h2
                x-text="selectedCategory.name"
                class="truncate text-lg font-black tracking-tight text-white"
              >
              </h2>
              <span
                class="text-[10px] text-white/70"
                x-text="`Digunakan oleh ${usedTransactionCount} transaksi`"
              ></span>
            </div>
          </div>
        </div>

        <div class="p-6">
          <div class="flex items-center gap-2">
            <button
              type="button"
              @click="attemptDeleteCategory()"
              class="flex w-1/3 cursor-pointer items-center justify-center gap-1.5 rounded-2xl border border-rose-200 bg-rose-50/80 py-3.5 text-xs font-bold text-rose-600 transition-colors active:scale-95"
            >
              <Trash2 class="h-4 w-4 stroke-2" />
              <span>Hapus</span>
            </button>

            <button
              type="button"
              @click="openEditFromDetail()"
              class="bg-primary text-primary-foreground shadow-soft flex w-2/3 cursor-pointer items-center justify-center gap-1.5 rounded-2xl py-3.5 text-xs font-bold transition-opacity hover:opacity-90 active:scale-95"
            >
              <Pencil class="h-4 w-4 stroke-2" />
              <span>Edit Kategori</span>
            </button>
          </div>
        </div>
      </div>
    </div>

    <!-- 6. MODAL REASSIGN KATEGORI -->
    <div
      x-show="openReassignModal"
      x-cloak
      class="bg-primary/20 fixed inset-0 z-50 flex items-end justify-center backdrop-blur-xs transition-opacity sm:items-center"
    >
      <div
        @click.away="openReassignModal = false"
        class="border-border bg-card w-full max-w-md space-y-5 rounded-t-4xl border p-6 pb-24 shadow-2xl sm:rounded-4xl"
      >
        <div class="flex items-center gap-3 text-rose-600">
          <div
            class="flex h-10 w-10 items-center justify-center rounded-2xl bg-rose-100"
          >
            <AlertTriangle class="h-5 w-5 stroke-2" />
          </div>

          <div>
            <h3 class="text-foreground text-sm font-extrabold">
              Kategori Tidak Bisa Dihapus
            </h3>

            <p
              class="text-muted-foreground text-[10px]"
              x-text="`Ada ${usedTransactionCount} transaksi menggunakan kategori ini.`"
            >
            </p>
          </div>
        </div>

        <div class="border-border bg-muted/40 rounded-2xl border p-4">
          <p class="text-muted-foreground text-xs leading-relaxed">
            Kategori
            <strong class="text-foreground" x-text="selectedCategory.name"
            ></strong>
            masih digunakan oleh
            <strong class="text-foreground" x-text="usedTransactionCount"
            ></strong>
            transaksi, jadi kategori ini tidak dapat dihapus.
          </p>
        </div>

        <p class="text-muted-foreground text-xs leading-relaxed">
          Kalau sudah tidak ingin menggunakan kategori ini, kami sarankan untuk
          <strong class="text-foreground"
            >mengubah nama atau mengedit kategori</strong
          >
          tersebut.
        </p>

        <div class="flex items-center gap-2 pt-2">
          <button
            type="button"
            @click="openReassignModal = false"
            class="border-border text-muted-foreground hover:bg-muted w-1/3 cursor-pointer rounded-2xl border py-3.5 text-xs font-bold"
          >
            Batal
          </button>

          <button
            type="button"
            @click="openReassignModal = false; openEditFromDetail()"
            class="w-2/3 cursor-pointer rounded-2xl bg-blue-600 py-3.5 text-xs font-bold text-white shadow-sm hover:bg-blue-500 active:scale-95"
          >
            Edit Kategori
          </button>
        </div>
      </div>
    </div>

    <!-- 7. MODAL FORM TAMBAH / EDIT KATEGORI -->
    <div
      x-show="openFormModal"
      x-cloak
      class="bg-primary/20 fixed inset-0 z-50 flex items-end justify-center backdrop-blur-xs transition-opacity sm:items-center"
    >
      <div
        @click.away="openFormModal = false"
        class="border-border bg-card flex max-h-[90vh] w-full max-w-md flex-col rounded-t-4xl border p-6 pb-24 shadow-2xl transition-transform sm:rounded-4xl"
      >
        <div
          class="border-border flex items-center justify-between border-b pb-3"
        >
          <div class="flex items-center gap-2">
            <div
              class="bg-blue-soft text-blue-main flex h-8 w-8 items-center justify-center rounded-xl"
            >
              <Sparkles class="h-4 w-4 stroke-2" />
            </div>
            <h3
              x-text="isEdit ? 'Edit Kategori' : 'Tambah Kategori Baru'"
              class="text-primary text-base font-extrabold"
            >
            </h3>
          </div>
          <button
            type="button"
            @click="openFormModal = false"
            class="text-muted-foreground hover:bg-muted flex h-8 w-8 items-center justify-center rounded-full"
          >
            <X class="h-4 w-4 stroke-[2.5]" />
          </button>
        </div>

        <form @submit.prevent="saveCategory()" class="mt-4 space-y-4">
          <!-- NAMA KATEGORI -->
          <div class="space-y-1.5">
            <label
              class="text-muted-foreground block text-[10px] font-bold uppercase"
              >Nama Kategori</label
            >
            <input
              type="text"
              x-model="formData.name"
              x-ref="categoryNameInput"
              placeholder="misal: Makanan, Pendidikan, Pulsa"
              class="border-border bg-background text-foreground focus:border-blue-main w-full rounded-2xl border px-4 py-3 text-xs font-semibold outline-none"
              required
            />
          </div>

          <!-- TIPE KATEGORI (SEGMENTED SELECTOR WITH AUTO ICON FILTER) -->
          <div class="space-y-1.5">
            <label
              class="text-muted-foreground block text-[10px] font-bold uppercase"
              >Tipe Transaksi</label
            >
            <div
              class="border-border bg-background grid grid-cols-2 gap-2 rounded-2xl border p-1"
            >
              <button
                type="button"
                @click="setFormType('expense')"
                :class="formData.type === 'expense' ? 'bg-rose-500 text-white shadow-xs' : 'text-muted-foreground hover:text-foreground'"
                class="flex cursor-pointer items-center justify-center gap-2 rounded-xl py-2.5 text-xs font-bold transition-all"
              >
                <TrendingDown class="h-3.5 w-3.5 stroke-[2.5]" />
                <span>Pengeluaran</span>
              </button>

              <button
                type="button"
                @click="setFormType('income')"
                :class="formData.type === 'income' ? 'bg-emerald-600 text-white shadow-xs' : 'text-muted-foreground hover:text-foreground'"
                class="flex cursor-pointer items-center justify-center gap-2 rounded-xl py-2.5 text-xs font-bold transition-all"
              >
                <TrendingUp class="h-3.5 w-3.5 stroke-[2.5]" />
                <span>Pemasukan</span>
              </button>
            </div>
          </div>

          <!-- IKON KATEGORI (VISUAL GRID PICKER FILTERED BY TYPE) -->
          <div class="space-y-1.5">
            <label
              class="text-muted-foreground block text-[10px] font-bold uppercase"
              >Pilih Ikon</label
            >

            <!-- Trigger Dropdown -->
            <!-- <button
              type="button"
              @click="openIconPicker = !openIconPicker"
              class="border-border bg-background text-foreground flex w-full cursor-pointer items-center justify-between rounded-2xl border px-4 py-2.5 transition-all"
            >
              <div class="flex items-center gap-3">
                <div
                  class="bg-blue-soft text-primary flex h-7 w-7 items-center justify-center rounded-xl"
                >
                  <template x-if="formData.icon === 'utensils'"
                    ><Utensils class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'car'"
                    ><Car class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'shopping-bag'"
                    ><ShoppingBag class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'gamepad-2'"
                    ><Gamepad2 class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'heart-pulse'"
                    ><HeartPulse class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'briefcase'"
                    ><Briefcase class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'gift'"
                    ><Gift class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'receipt'"
                    ><Receipt class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'circle-ellipsis'"
                    ><CircleEllipsis class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'trending-up'"
                    ><TrendingUp class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'wallet'"
                    ><Wallet class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'zap'"
                    ><Zap class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'droplets'"
                    ><Droplets class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'wifi'"
                    ><Wifi class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'home'"
                    ><Home class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'shirt'"
                    ><Shirt class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'coffee'"
                    ><Coffee class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'film'"
                    ><Film class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'dumbbell'"
                    ><Dumbbell class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'scissors'"
                    ><Scissors class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'graduation-cap'"
                    ><GraduationCap class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'baby'"
                    ><Baby class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'paw-print'"
                    ><PawPrint class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'plane'"
                    ><Plane class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'fuel'"
                    ><Fuel class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'wrench'"
                    ><Wrench class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'piggy-bank'"
                    ><PiggyBank class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'credit-card'"
                    ><CreditCard class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'coins'"
                    ><Coins class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'building'"
                    ><Building class="h-4 w-4 stroke-2" /></template
                  >
                  <template x-if="formData.icon === 'tag'"
                    ><Tag class="h-4 w-4 stroke-2" /></template
                  >
                </div>
                <span
                  class="text-xs font-semibold capitalize"
                  x-text="availableIcons.find(i => i.id === formData.icon)?.name || 'Pilih Icon'"
                ></span>
              </div>

              <ChevronDown
                class="text-muted-foreground h-4 w-4 transition-transform"
                :class="openIconPicker ? 'rotate-180' : ''"
              />
            </button> -->

            <!-- Expandable Icon Grid (Filtered for Active Type) -->
            <div
              x-show="openIconPicker"
              class="border-border bg-muted/20 mt-3 h-40 overflow-y-auto rounded-2xl border p-3"
            >
              <div class="grid grid-cols-6 gap-2">
                <template x-for="item in filteredAvailableIcons" :key="item.id">
                  <button
                    type="button"
                    @click="formData.icon = item.id"
                    :class=`formData.icon === item.id
          ? 'bg-blue-soft text-blue border-blue'
          : 'bg-card text-muted-foreground border-border hover:bg-muted/50'`
                    class="flex h-11 w-11 items-center justify-center rounded-xl border transition-colors"
                    :title="item.name"
                  >
                    <template x-if="item.id === 'utensils'">
                      <Utensils :size="18" />
                    </template>

                    <template x-if="item.id === 'car'">
                      <Car :size="18" />
                    </template>

                    <template x-if="item.id === 'shopping-bag'">
                      <ShoppingBag :size="18" />
                    </template>

                    <template x-if="item.id === 'gamepad-2'">
                      <Gamepad2 :size="18" />
                    </template>

                    <template x-if="item.id === 'heart-pulse'">
                      <HeartPulse :size="18" />
                    </template>

                    <template x-if="item.id === 'receipt'">
                      <Receipt :size="18" />
                    </template>

                    <template x-if="item.id === 'zap'">
                      <Zap :size="18" />
                    </template>

                    <template x-if="item.id === 'droplets'">
                      <Droplets :size="18" />
                    </template>

                    <template x-if="item.id === 'wifi'">
                      <Wifi :size="18" />
                    </template>

                    <template x-if="item.id === 'home'">
                      <Home :size="18" />
                    </template>

                    <template x-if="item.id === 'coffee'">
                      <Coffee :size="18" />
                    </template>

                    <template x-if="item.id === 'shirt'">
                      <Shirt :size="18" />
                    </template>

                    <template x-if="item.id === 'film'">
                      <Film :size="18" />
                    </template>

                    <template x-if="item.id === 'dumbbell'">
                      <Dumbbell :size="18" />
                    </template>

                    <template x-if="item.id === 'scissors'">
                      <Scissors :size="18" />
                    </template>

                    <template x-if="item.id === 'graduation-cap'">
                      <GraduationCap :size="18" />
                    </template>

                    <template x-if="item.id === 'baby'">
                      <Baby :size="18" />
                    </template>

                    <template x-if="item.id === 'paw-print'">
                      <PawPrint :size="18" />
                    </template>

                    <template x-if="item.id === 'plane'">
                      <Plane :size="18" />
                    </template>

                    <template x-if="item.id === 'fuel'">
                      <Fuel :size="18" />
                    </template>

                    <template x-if="item.id === 'wrench'">
                      <Wrench :size="18" />
                    </template>

                    <template x-if="item.id === 'credit-card'">
                      <CreditCard :size="18" />
                    </template>

                    <template x-if="item.id === 'briefcase'">
                      <Briefcase :size="18" />
                    </template>

                    <template x-if="item.id === 'gift'">
                      <Gift :size="18" />
                    </template>

                    <template x-if="item.id === 'trending-up'">
                      <TrendingUp :size="18" />
                    </template>

                    <template x-if="item.id === 'wallet'">
                      <Wallet :size="18" />
                    </template>

                    <template x-if="item.id === 'piggy-bank'">
                      <PiggyBank :size="18" />
                    </template>

                    <template x-if="item.id === 'coins'">
                      <Coins :size="18" />
                    </template>

                    <template x-if="item.id === 'building'">
                      <Building :size="18" />
                    </template>

                    <template x-if="item.id === 'circle-ellipsis'">
                      <CircleEllipsis :size="18" />
                    </template>
                  </button>
                </template>
              </div>
            </div>
          </div>

          <div class="flex items-center gap-2 pt-2">
            <button
              type="button"
              @click="openFormModal = false"
              class="border-border text-muted-foreground hover:bg-muted w-1/3 cursor-pointer rounded-2xl border py-3.5 text-xs font-bold"
              >Batal</button
            >
            <button
              type="submit"
              class="bg-primary text-primary-foreground shadow-soft w-2/3 cursor-pointer rounded-2xl py-3.5 text-xs font-bold hover:opacity-90 active:scale-95"
              >Simpan Kategori</button
            >
          </div>
        </form>
      </div>
    </div>
  </div>
</Layout>
